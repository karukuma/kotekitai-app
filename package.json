{
  "name": "firestore-sheets-sync",
  "version": "1.0.0",
  "description": "Firestore to Google Sheets sync with Vercel Functions",
  "main": "index.js",
  "scripts": {
    "start": "node -e \"console.log('Server ready at http://localhost:3000')\"",
    "build": "mkdir -p public && echo 'Build completed - static files ready'",
    "deploy": "npx vercel --prod"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "googleapis": "^128.0.0"
  },
  "engines": {
    "node": "22.x"
  }
}